# 论文实验部分使用指南

完整的实验部分LaTeX代码已生成，虽然实验结果还未填充，但框架已完整。

## 📁 生成的文件

1. **paper_experiments.tex** - 完整的实验部分LaTeX代码
2. **generate_paper_results.py** - 结果收集和格式化脚本

## 📝 论文实验部分包含内容

### 1. Experimental Setup（实验设置）
- ✅ 数据集描述（Amazon-Book, Last-FM, Yelp2018）
- ✅ 评估指标（Recall@K, NDCG@K, Hit@K, Precision@K）
- ✅ 基线方法（10个主流方法）
- ✅ 实现细节（完整的超参数配置）

### 2. Overall Performance (RQ1)（整体性能）
- ✅ 主实验结果表格（12列 × 所有方法）
- ⏳ 需填充：0.XXX 的数值
- ⏳ 需填充：提升百分比 X.X%

### 3. Ablation Study (RQ2)（消融实验）
- ✅ 6个消融变体的设计
- ⏳ 需填充：消融实验结果

### 4. Hyperparameter Sensitivity (RQ3)（超参数分析）
- ✅ 候选集大小分析
- ✅ 嵌入维度分析
- ✅ LSH配置分析
- ⏳ 需填充：实验数据
- ⏳ 需生成：图表（Figure 1）

### 5. Efficiency Analysis (RQ4)（效率分析）
- ✅ 训练时间对比表格
- ✅ 推理延迟对比表格
- ⏳ 需填充：时间数据

### 6. Case Study（案例研究）
- ✅ 定性分析样例
- ✅ 注意力可视化设计
- ⏳ 需填充：真实案例
- ⏳ 需生成：可视化图表（Figure 2）

### 7. Discussion（讨论）
- ✅ 方法有效性分析
- ✅ 局限性和未来工作

## 🚀 使用流程

### Step 1: 直接使用（论文写作）

```latex
% 在你的论文主文件中：
\input{paper_experiments.tex}
```

当前版本可以作为**实验部分的完整框架**，包含：
- 所有表格结构
- 方法描述
- 分析文字
- 占位符（0.XXX）等结果出来后填充

### Step 2: 运行实验（获取结果）

```bash
# 1. 上传到AutoDL并配置环境
bash autodl_setup_5090.sh

# 2. 运行主实验
bash start_training_5090.sh full

# 3. 运行消融实验
python train_5090_optimized.py --save-dir ablation_no_kg \
    --kg-emb-dim 0  # 模拟没有KG

python train_5090_optimized.py --save-dir ablation_no_lsh \
    --candidate-size 99999  # 使用全部候选

# 4. 记录结果
# 从训练日志和评估输出中提取指标
```

### Step 3: 填充结果

方案A：**手动填充**
```latex
% 在 paper_experiments.tex 中搜索 "0.XXX"
% 手动替换为实际数值

\textbf{RA-KG-PPO (Ours)} & \textbf{0.0856} & \textbf{0.0645} & ...
```

方案B：**使用脚本**
```bash
# 1. 编辑结果文件
python generate_paper_results.py

# 这会生成 paper_results_template.json
# 填充该JSON文件

# 2. 重新运行生成LaTeX代码
python generate_paper_results.py > latex_tables.txt

# 3. 复制生成的LaTeX代码到论文中
```

### Step 4: 生成图表

```python
# 需要手动创建图表脚本（示例）

import matplotlib.pyplot as plt

# Figure 1: Candidate Size vs Performance
candidate_sizes = [50, 100, 150, 200, 250, 300]
recall_scores = [0.074, 0.081, 0.084, 0.086, 0.087, 0.087]

plt.figure(figsize=(6, 4))
plt.plot(candidate_sizes, recall_scores, 'o-', linewidth=2)
plt.xlabel('Candidate Set Size')
plt.ylabel('Recall@20')
plt.grid(True)
plt.savefig('figures/candidate_size.pdf', bbox_inches='tight')
```

## 📊 实验结果示例（模拟数据）

运行辅助脚本查看示例：

```bash
python generate_paper_results.py
```

输出示例：
```
================================================================================
Main Results Table - amazon-book
================================================================================

Recall@20:
  BPR            : 0.061
  NCF            : 0.064
  ...
  KGAT           : 0.078 (second best)
  RA-KG-PPO      : 0.086 (BEST)

Improvement: +9.5% over KGAT
```

## 🎯 当前状态总结

### ✅ 已完成（可直接用于论文）

- [x] 完整的实验部分结构
- [x] 所有章节和子章节
- [x] 表格框架（12个表格）
- [x] 方法描述和分析文字
- [x] 讨论和局限性
- [x] LaTeX格式正确（可编译）

### ⏳ 待完成（需要实验结果）

- [ ] 主实验结果数值（Table 2）
- [ ] 消融实验数值（Table 3）
- [ ] 超参数分析数值（Table 4-6）
- [ ] 效率分析数值（Table 7-8）
- [ ] 两个图表（Figure 1-2）
- [ ] 案例研究的真实例子

### 📈 填充优先级

1. **高优先级**（主要贡献）
   - Table 2: 主实验结果对比
   - Table 3: 消融实验
   - 提升百分比计算

2. **中优先级**（支撑性实验）
   - Table 4-6: 超参数分析
   - Figure 1: 候选集大小曲线

3. **低优先级**（锦上添花）
   - Table 7-8: 效率分析
   - Table 9: 案例研究
   - Figure 2: 可视化

## 💡 写作建议

### 现在可以做的

1. **撰写其他章节**
   ```latex
   \section{Introduction}
   \section{Related Work}
   \section{Methodology}  % 基于你的代码描述
   \section{Experiments}  % 使用 paper_experiments.tex
   \section{Conclusion}
   ```

2. **完善方法描述**
   - 从代码中提取算法流程
   - 绘制架构图
   - 写清楚创新点

3. **准备实验脚本**
   - 确保所有消融变体可以运行
   - 准备基线方法的实现或论文数据

### 等结果出来后

1. **快速填充表格**
   ```bash
   # 使用辅助脚本批量替换
   sed -i 's/0.XXX/实际数值/g' paper_experiments.tex
   ```

2. **生成图表**
   - 使用 matplotlib/seaborn
   - 保存为 PDF 格式（高质量）

3. **完善分析**
   - 根据实际结果调整 Discussion
   - 添加具体的观察和洞察

## 📚 LaTeX编译

```bash
# 确保你的论文主文件包含：
\usepackage{booktabs}  % 表格线
\usepackage{multirow}  % 合并单元格
\usepackage{graphicx}  % 图片

% 然后编译：
pdflatex main.tex
bibtex main
pdflatex main.tex
pdflatex main.tex
```

## ❓ 常见问题

**Q1: 表格太宽怎么办？**
```latex
% 已使用 \resizebox 自适应页面宽度
\resizebox{\textwidth}{!}{
  \begin{tabular}{...}
  ...
  \end{tabular}
}
```

**Q2: 如何添加更多基线？**
```latex
% 在表格中添加一行
NewBaseline & 0.XXX & 0.XXX & ... \\
```

**Q3: 数据集统计数字需要改吗？**
```latex
% Table 1 中的统计已基于amazon-book数据集
% 如果使用其他数据集，需要更新这些数字
```

**Q4: 如何引用这些表格？**
```latex
% 在论文正文中：
As shown in Table~\ref{tab:main_results}, ...
Figure~\ref{fig:candidate_size} illustrates ...
```

## 🎓 完整示例

查看 `paper_experiments.tex` 获取：
- 完整的LaTeX代码
- 所有表格和图表引用
- 实验描述和分析文字
- 占位符说明（文件末尾的注释）

## 📞 需要帮助？

如果实验结果出来后需要：
- 批量填充表格
- 生成可视化图表
- 调整表格格式
- 添加新的分析

随时告诉我！

---

**当前版本**: 可直接用于论文写作，结构完整
**最后更新**: 2025-12-20
